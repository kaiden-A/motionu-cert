# Node modules (we install them inside Docker)
node_modules
npm-debug.log
yarn-error.log
.pnpm-store

# Built folders (handled inside Docker)
dist

# Environment files
.env
.env.local
.env.*.local

# Git files
.git
.gitignore

# IDE/editor files
.vscode
.idea
*.swp

# NOTE: src/fonts/ is intentionally NOT ignored
# NOTE: generated/ is intentionally NOT ignored
# Prisma needs the schema to generate the client inside Docker